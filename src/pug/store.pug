-
  const store = {
    pages: [
      {
        url: 'index',
        title: 'Главная',
      },
      {
        url: 'event-create',
        title: 'Создание события',
      },
    ],
    getPage(url) {
      return this.pages.find((page) => page.url === url);
    },
  };
