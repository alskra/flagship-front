mixin header
  header.header(x-data="header")&attributes(attributes)
    .container.grid-12.header__container
      //-+btn({ iconPrev: 'list' }).header__btn-nav(x-on:click="toggleNav")
      .col-span-3.col-span-6-max-lg
        a.header__logo(href="/")
          img(
            src="~@images/logo.png?as=webp"
            srcset="~@images/logo@1.5x.png?as=webp 1.5x, ~@images/logo@2x.png?as=webp 2x"
            loading="lazy"
            alt=""
          )
      .col-span-6.header__nav-box(x-bind="nav")
        nav.header__nav
          a.header__nav-item(href="/") Главная
          a.header__nav-item(href="") Пункт меню 2
          a.header__nav-item(href="") Пункт меню 3
      .col-span-3.col-span-6-max-lg.header__actions
        +btn({ tag: 'a', content: 'Выйти', icons: ['sign-out'] }).header__btn.header__btn--primary.header__btn--sign
        +btn({ tag: 'a', icons: ['profile'] }).header__btn.header__btn--accent
